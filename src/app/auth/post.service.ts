import { Injectable } from '@angular/core';
import { Post } from '../shared/post-model';

@Injectable({
  providedIn: 'root'
})
export class PostService {
  // posts: Post[] = [new Post('', '', 29, 1, 12, 15)];
  constructor() { }

  // onCreatePost(post: Post) {
  //   this.posts.push(post)
  // }
}
